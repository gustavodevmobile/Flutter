import 'package:flutter/material.dart';

class CadastroProfissionalScreen extends StatelessWidget {
  const CadastroProfissionalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Cadastro Profissional')),
      body: Center(child: Text('Tela de Cadastro Profissional')),
    );
  }
}
