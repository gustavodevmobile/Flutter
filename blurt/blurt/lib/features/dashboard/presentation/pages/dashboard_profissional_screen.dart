import 'package:flutter/material.dart';

class DashboardProfissionalScreen extends StatelessWidget {
  const DashboardProfissionalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Dashboard Profissional')),
      body: Center(child: Text('Tela de Dashboard Profissional')),
    );
  }
}
